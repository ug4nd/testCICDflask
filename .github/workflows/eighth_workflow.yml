name: EighthWorkflow
on:
  workflow_dispatch
  
jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Display Event arguments Github
        run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF
