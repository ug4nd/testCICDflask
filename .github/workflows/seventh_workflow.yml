
name: seventhWorkflow
on:
  schedule:
    - cron: "20-59/8 20 * * *"
jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Display Event arguments Github
        run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF
