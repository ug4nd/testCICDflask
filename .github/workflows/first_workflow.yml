name: FirstWorkflow
on: workflow_dispatch

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Display some Text
        run: echo "Some Text"
      - name: Command in Multiline
        run: |
          node -v
          npm -v