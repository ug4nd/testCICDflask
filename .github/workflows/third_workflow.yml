name: ThirdWorkflow
on: push

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: List files
        run: |
          ls -la
      #- uses: actions/checkout@v4
      - name: Copy Files
        run: |
          git init
          git remote add origin $GITHUB_ACTOR:${{secrets.GITHUB_TOKEN}}@github.com:ug4nd/$GITHUB_REPOSITORY.git
          git fetch
          git checkout main
      - name: List files
        run: |
          ls -la      